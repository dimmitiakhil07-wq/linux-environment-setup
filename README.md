# linux-environment-setup
This project demonstrates how to set up a Linux environment from scratch using Ubuntu inside a virtual machine. The setup uses Oracle VM VirtualBox to run Ubuntu safely on a host operating system without affecting the host machine.  This guide documents the full process including downloading software, creating a virtual machine, installing Ubuntu.
